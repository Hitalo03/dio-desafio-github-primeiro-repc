Algoritmo "Tabuada"

Var

   x, y, N, tabuada : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor? ")
   leia(x)
   
   N <- 0

   para y de 1 ate 10 faca

      N <- n + 1
      tabuada <- x * N
      escreval(x, " x ", N, " = ", tabuada)
      
   fimpara
      

Fimalgoritmo